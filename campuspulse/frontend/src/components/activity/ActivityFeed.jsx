/**
 * Activity feed list. Consumes useActivities() and renders ActivityCards,
 * loading skeletons, or an empty state. Stage 4 fills in the implementation.
 */
export function ActivityFeed() {
  return null
}
