/**
 * Single activity card. Renders tag, title, location, spot counter,
 * expiry countdown, and the join button. Stage 4 fills in the implementation.
 */
export function ActivityCard() {
  return null
}
